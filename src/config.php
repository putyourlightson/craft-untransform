<?php
/**
 * @copyright Copyright (c) PutYourLightsOn
 */

/**
 * Untransform config.php
 *
 * This file exists only as a template for the Untransform settings.
 * It does nothing on its own.
 *
 * Don't edit this file, instead copy it to 'craft/config' as 'untransform.php'
 * and make your changes there to override default settings.
 *
 * Once copied to 'craft/config', this file will be multi-environment aware as
 * well, so you can have different settings groups for each environment, just as
 * you do for 'general.php'
 */

return [
    // How image transform URLs should be replaced. included/excluded URI patterns. Disable this setting to prevent Blitz from caching any new pages.
    //'replaceTransforms' => 0,

    // The URL of the placeholder image to replace transformed images with. Leave this blank to use the default placeholder image.
    //'placeholderImage' => '',

    // The URL prefix to prepend to the volume's base URL.
    //'baseUrlPrefix' => '',
];
